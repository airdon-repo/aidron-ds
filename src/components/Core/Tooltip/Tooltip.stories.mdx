import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { Tooltip } from './Tooltip';
import { Button } from '../Button';

<Meta
  title="Core/Tooltip/Docs"
  parameters={{ previewTabs: { 'canvas': { hidden: true }}}}
/>

# Tooltip
Text fields possibilitam que usuários insiram textos em uma interface.

<Canvas>
  {['top', 'bottom', 'left', 'right'].map((x) => (
    <Button
      key={x.toString()}
      className="tooltip"
    >
      Hover me
      <Tooltip direction={x}>Tooltip&apos;s Text</Tooltip>
    </Button>
  ))}
</Canvas>

## Anatomia
* Container
* Trigger

## Design Tokens
`$font-family-secondary`
`$spacing-stack-xxs`
`$spacing-stack-xs`
`$spacing-inset-xs`
`$border-radius-sm`

## Variações
* Light Mode.
* Dark Mode.